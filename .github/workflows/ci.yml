name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --extra dev
      - run: uv run ruff check src/ tests/
      - run: uv run ruff format --check src/ tests/

  type-check:
    name: Type Check
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --extra dev
      - run: uv run mypy --ignore-missing-imports src/
        continue-on-error: true

  security:
    name: Security
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --extra dev
      - run: uv pip install pip-audit bandit
      - name: Audit dependencies for CVEs
        # Known CVEs with fixes in major version bumps only:
        #   CVE-2026-28277 → langgraph 1.0.10 (current: 0.6.x)
        #   CVE-2026-27794 → langgraph-checkpoint 4.0.0 (current: 3.0.x)
        #   CVE-2026-25645 → requests 2.33.0 (current: 2.32.x)
        # Revisit when these dependencies are upgraded.
        run: >
          uv run pip-audit
          --ignore-vuln CVE-2026-28277
          --ignore-vuln CVE-2026-27794
          --ignore-vuln CVE-2026-25645
      - name: Static security analysis
        run: uv run bandit -r src/ -ll --quiet

  test:
    name: Test (Python ${{ matrix.python-version }})
    needs: [type-check, security]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.13"]
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --extra dev
      - name: Run tests with coverage
        run: >
          uv run pytest tests/ -v --tb=short -x
          --cov=src --cov-report=term-missing
          --cov-fail-under=50
          --junitxml=test-results.xml
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.python-version }}
          path: test-results.xml
